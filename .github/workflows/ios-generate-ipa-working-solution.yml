name: Generate iOS Simulator IPA (Working Solution)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  generate-ipa:
    runs-on: macos-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Xcode
      run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer
      
    - name: Install dependencies
      run: |
        echo "📦 Resolving dependencies..."
        xcodebuild -resolvePackageDependencies -project VividAI.xcodeproj
        
    - name: Clean environment
      run: |
        echo "🧹 Cleaning build environment..."
        rm -rf ~/Library/Developer/Xcode/DerivedData/*
        rm -rf ./DerivedData
        rm -rf ./build
        
    - name: List available simulators
      run: |
        echo "📱 Available simulators:"
        xcrun simctl list devices available
        
    - name: Create and boot iOS Simulator
      run: |
        echo "📱 Creating and booting iOS Simulator..."
        
        # List available runtimes first
        echo "📱 Available iOS runtimes:"
        xcrun simctl list runtimes | grep iOS
        
        # Use available iOS runtime (usually latest)
        IOS_RUNTIME=$(xcrun simctl list runtimes | grep iOS | tail -1 | awk '{print $NF}')
        echo "📱 Using iOS runtime: $IOS_RUNTIME"
        
        # Create simulator with available runtime
        xcrun simctl create "VividAI-Test" "iPhone 16" "$IOS_RUNTIME" || echo "Simulator already exists"
        
        # Boot simulator without timeout (use gtimeout if available, otherwise just boot)
        if command -v gtimeout >/dev/null 2>&1; then
          gtimeout 30 xcrun simctl boot "VividAI-Test" || echo "Simulator boot timeout or already booted"
        else
          xcrun simctl boot "VividAI-Test" || echo "Simulator already booted"
        fi
        
        # Wait for simulator to be ready
        xcrun simctl bootstatus "VividAI-Test" -b || echo "Simulator status check failed"
        
    - name: Build for iOS Device using SKETCHAI SUCCESS PATTERN
      run: |
        echo "🔨 Building for iOS Device using SKETCHAI SUCCESS PATTERN..."
        echo "📱 Target: iOS Device (not simulator)"
        echo "📱 App: VividAI (Firebase-integrated complex app)"
        
        set -e
        
        echo "📁 Current directory contents before build:"
        ls -la ./
        
        # Kill any lingering simulator processes that could cause runtime conflicts
        echo "🔧 Killing lingering simulator processes..."
        sudo killall -9 com.apple.CoreSimulator.CoreSimulatorService 2>/dev/null || true
        
        # SKETCHAI SUCCESS PATTERN: Build for device using generic/platform=iOS
        echo "🔨 Starting xcodebuild archive for iOS Device (SKETCHAI SUCCESS PATTERN)..."
        xcodebuild archive \
          -project VividAI.xcodeproj \
          -scheme VividAI \
          -destination 'generic/platform=iOS' \
          -configuration Release \
          -archivePath ./VividAI.xcarchive \
          DEVELOPMENT_TEAM="" \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          PROVISIONING_PROFILE="" \
          ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS=NO \
          ASSETCATALOG_COMPILER_OPTIMIZATION=space \
          ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS=NO \
          ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOLS=NO \
          SUPPORTED_PLATFORMS="iphoneos" \
          VALID_ARCHS="arm64" \
          ARCHS="arm64" \
          EXCLUDED_ARCHS[sdk=iphonesimulator*]="arm64" \
          -allowProvisioningUpdates \
          -resolvePackageDependencies \
          -verbose \
          -showBuildTimingSummary
        
        echo "✅ Archive completed successfully!"
        
        echo "📁 Current directory contents after archive:"
        ls -la ./
        
        echo "📁 Looking for any .xcarchive files:"
        find . -name "*.xcarchive" -type d || echo "No .xcarchive files found"
        
        echo "📁 Looking for any .app files anywhere:"
        find . -name "*.app" -type d || echo "No .app files found"
        
        echo "📁 Looking for any build products:"
        find . -name "*VividAI*" -type d || echo "No VividAI products found"
        
        echo "📁 Looking in DerivedData:"
        find ./DerivedData -name "*.app" -type d || echo "No .app files in DerivedData"
        
        echo "📁 Looking in build directories:"
        find . -name "*Build*" -type d || echo "No Build directories found"
        
        echo "📁 Looking in Products directories:"
        find . -name "*Products*" -type d || echo "No Products directories found"
        
    - name: Find app bundle using COMPREHENSIVE SEARCH
      run: |
        echo "🔍 Looking for app bundle using COMPREHENSIVE SEARCH..."
        
        # Search in multiple locations for app bundle
        echo "📁 Searching for app bundle in all possible locations..."
        
        # Method 1: Look for app bundle in archive (if it exists)
        APP_PATH=$(find ./VividAI.xcarchive -name "VividAI.app" -type d | head -1)
        
        if [ -z "$APP_PATH" ]; then
          echo "❌ App bundle not found in archive"
          
          # Method 2: Look for app bundle in DerivedData
          echo "📁 Searching in DerivedData..."
          APP_PATH=$(find ./DerivedData -name "VividAI.app" -type d | head -1)
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ App bundle not found in DerivedData"
            
            # Method 3: Look for any .app files anywhere
            echo "📁 Searching for any .app files anywhere..."
            APP_PATH=$(find . -name "*.app" -type d | head -1)
            
            if [ -z "$APP_PATH" ]; then
              echo "❌ No .app files found anywhere"
              
              # Method 4: Look for VividAI products
              echo "📁 Searching for VividAI products..."
              APP_PATH=$(find . -name "*VividAI*" -type d | head -1)
              
              if [ -z "$APP_PATH" ]; then
                echo "❌ No VividAI products found"
                
                # Method 5: Look in build directories
                echo "📁 Searching in build directories..."
                APP_PATH=$(find . -name "*Build*" -type d | head -1)
                
                if [ -z "$APP_PATH" ]; then
                  echo "❌ No build directories found"
                  
                  # Method 6: Look in Products directories
                  echo "📁 Searching in Products directories..."
                  APP_PATH=$(find . -name "*Products*" -type d | head -1)
                  
                  if [ -z "$APP_PATH" ]; then
                    echo "❌ No Products directories found"
                    echo "📁 Final search - all files:"
                    find . -type f | head -20 || echo "No files found"
                    exit 1
                  else
                    echo "✅ Found Products directory: $APP_PATH"
                    # Look for app bundle in Products directory
                    APP_PATH=$(find "$APP_PATH" -name "VividAI.app" -type d | head -1)
                    if [ -z "$APP_PATH" ]; then
                      echo "❌ No VividAI.app found in Products directory"
                      exit 1
                    fi
                  fi
                else
                  echo "✅ Found build directory: $APP_PATH"
                  # Look for app bundle in build directory
                  APP_PATH=$(find "$APP_PATH" -name "VividAI.app" -type d | head -1)
                  if [ -z "$APP_PATH" ]; then
                    echo "❌ No VividAI.app found in build directory"
                    exit 1
                  fi
                fi
              else
                echo "✅ Found VividAI product: $APP_PATH"
                # Check if it's an app bundle
                if [ ! -f "$APP_PATH/Info.plist" ]; then
                  echo "❌ VividAI product is not an app bundle"
                  exit 1
                fi
              fi
            else
              echo "✅ Found .app file: $APP_PATH"
              # Check if it's VividAI app
              if [[ "$APP_PATH" != *"VividAI.app" ]]; then
                echo "❌ Found .app file but it's not VividAI.app"
                exit 1
              fi
            fi
          else
            echo "✅ App bundle found in DerivedData: $APP_PATH"
          fi
        else
          echo "✅ App bundle found in archive: $APP_PATH"
        fi
        
        # SKETCHAI SUCCESS PATTERN: App bundle should be found in archive
        # No need for additional search methods since we're using device build
        
        echo "✅ App bundle found at: $APP_PATH"
        echo "📱 App bundle size: $(du -sh "$APP_PATH")"
        
        # Verify app bundle contents
        echo "📱 App bundle contents:"
        ls -la "$APP_PATH"
        
        # Verify it's a valid app bundle
        if [ ! -f "$APP_PATH/Info.plist" ]; then
          echo "❌ Invalid app bundle - missing Info.plist"
          exit 1
        fi
        
        echo "✅ App bundle is valid"
        
        # Set APP_PATH as an output for subsequent steps
        echo "APP_PATH=$APP_PATH" >> $GITHUB_ENV
        
    - name: Create IPA file
      run: |
        echo "📦 Creating IPA file..."
        
        # Create Payload directory
        mkdir -p ./Payload
        
        # Copy app bundle to Payload
        cp -R "$APP_PATH" ./Payload/
        
        # Create IPA file
        zip -r VividAI-Simulator.ipa ./Payload
        
        echo "✅ IPA file created: VividAI-Simulator.ipa"
        echo "📱 IPA file size: $(du -sh VividAI-Simulator.ipa)"
        
    - name: Verify IPA file
      run: |
        echo "🔍 Verifying IPA file..."
        
        if [ ! -f "VividAI-Simulator.ipa" ]; then
          echo "❌ IPA file not found!"
          exit 1
        fi
        
        echo "✅ IPA file exists"
        echo "📱 IPA file size: $(du -sh VividAI-Simulator.ipa)"
        
        # List contents of IPA
        echo "📦 IPA contents:"
        unzip -l VividAI-Simulator.ipa | head -20
        
    - name: Upload IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: VividAI-Simulator-IPA
        path: VividAI-Simulator.ipa
        retention-days: 30
        
    - name: Cleanup
      run: |
        echo "🧹 Cleaning up..."
        rm -rf ./Payload
        rm -rf ./DerivedData
        rm -rf ./build
        
        # Shutdown simulator
        xcrun simctl shutdown "VividAI-Test" || echo "Simulator already shutdown"
        xcrun simctl delete "VividAI-Test" || echo "Simulator already deleted"
