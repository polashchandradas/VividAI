name: Generate IPA File (Fixed)

on:
  workflow_dispatch:  # Manual trigger
  push:
    branches: [ main ]

jobs:
  generate-ipa:
    runs-on: macos-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Xcode
      run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer
      
    - name: Install dependencies
      run: |
        # Install CocoaPods if needed
        if [ -f "Podfile" ]; then
          gem install cocoapods
          pod install
        fi
        
        # Resolve Swift Package Manager dependencies
        xcodebuild -resolvePackageDependencies -project VividAI.xcodeproj
        
    - name: Create custom ExportOptions.plist for ad-hoc distribution
      run: |
        cat > ExportOptions.plist << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>method</key>
            <string>ad-hoc</string>
            <key>uploadBitcode</key>
            <false/>
            <key>uploadSymbols</key>
            <true/>
            <key>compileBitcode</key>
            <false/>
            <key>signingStyle</key>
            <string>automatic</string>
            <key>signingCertificate</key>
            <string>iOS Distribution</string>
            <key>provisioningProfiles</key>
            <dict>
                <key>com.vividai.app</key>
                <string>match AdHoc com.vividai.app</string>
            </dict>
        </dict>
        </plist>
        EOF
        
    - name: Build and archive VividAI
      run: |
        xcodebuild clean archive \
          -project VividAI.xcodeproj \
          -scheme VividAI \
          -destination 'generic/platform=iOS' \
          -archivePath VividAI.xcarchive \
          -configuration Release \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          DEVELOPMENT_TEAM="" \
          PROVISIONING_PROFILE_SPECIFIER=""
          
    - name: Export IPA with custom options
      run: |
        # Try to export with custom options
        xcodebuild -exportArchive \
          -archivePath VividAI.xcarchive \
          -exportOptionsPlist ExportOptions.plist \
          -exportPath ./build \
          -allowProvisioningUpdates || echo "Export failed, trying alternative method"
          
    - name: Alternative - Create unsigned IPA
      if: failure()
      run: |
        echo "Creating unsigned IPA for testing purposes..."
        mkdir -p ./build
        
        # Extract the app from archive
        cp -r VividAI.xcarchive/Products/Applications/VividAI.app ./build/
        
        # Create a simple IPA structure
        mkdir -p ./build/Payload
        cp -r ./build/VividAI.app ./build/Payload/
        
        # Create the IPA file
        cd ./build
        zip -r VividAI-unsigned.ipa Payload/
        cd ..
        
        echo "📱 Unsigned IPA created for testing!"
        
    - name: List generated files
      run: |
        echo "📱 Generated files:"
        ls -la ./build/
        if [ -f "./build/VividAI.ipa" ]; then
          echo "✅ Signed IPA file created!"
        elif [ -f "./build/VividAI-unsigned.ipa" ]; then
          echo "✅ Unsigned IPA file created!"
        else
          echo "❌ No IPA file found"
        fi
        
    - name: Upload IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: VividAI-ipa
        path: ./build/*.ipa
        retention-days: 30
        
    - name: Success message
      run: |
        echo "🎉 VividAI IPA file generated successfully!"
        echo "📱 You can download it from the Actions artifacts"
        echo "🔗 Go to Actions tab → Select workflow run → Download artifact"
        echo "💡 Upload this IPA to TestFlight, Appetize.io, or BrowserStack for testing"
