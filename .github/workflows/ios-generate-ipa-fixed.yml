name: Generate iOS Simulator IPA (Fixed)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  generate-ipa:
    runs-on: macos-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Xcode
      run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer
      
    - name: Install dependencies
      run: |
        echo "📦 Resolving dependencies..."
        xcodebuild -resolvePackageDependencies -project VividAI.xcodeproj
        
    - name: Clean environment
      run: |
        echo "🧹 Cleaning build environment..."
        rm -rf ~/Library/Developer/Xcode/DerivedData/*
        rm -rf ./DerivedData
        rm -rf ./build
        
    - name: Boot iOS Simulator
      run: |
        echo "📱 Booting iOS Simulator..."
        xcrun simctl boot "iPhone 16" || true
        xcrun simctl bootstatus "iPhone 16" -b
        
    - name: Build for iOS Simulator using archive
      run: |
        echo "🔨 Building for iOS Simulator using archive..."
        echo "📱 Target: iPhone 16, iOS 18.4"
        
        set -e
        
        # Use archive command which creates proper app bundles
        xcodebuild archive \
          -project VividAI.xcodeproj \
          -scheme VividAI \
          -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.4' \
          -configuration Release \
          -archivePath ./VividAI.xcarchive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          -resolvePackageDependencies \
          -verbose \
          -showBuildTimingSummary
        
        echo "✅ Archive completed successfully!"
        
    - name: Find and verify app bundle in archive
      run: |
        echo "🔍 Looking for app bundle in archive..."
        
        # Check if archive was created
        if [ ! -d "./VividAI.xcarchive" ]; then
          echo "❌ Archive not found!"
          exit 1
        fi
        
        echo "✅ Archive found at: ./VividAI.xcarchive"
        
        # Find app bundle in archive
        APP_PATH=$(find ./VividAI.xcarchive/Products/Applications -name "VividAI.app" -type d | head -1)
        
        if [ -z "$APP_PATH" ]; then
          echo "❌ App bundle not found in archive"
          echo "📁 Archive contents:"
          find ./VividAI.xcarchive -name "*.app" -type d || echo "No .app files found in archive"
          exit 1
        fi
        
        echo "✅ App bundle found in archive at: $APP_PATH"
        echo "📱 App bundle size: $(du -sh "$APP_PATH")"
        
        # Verify app bundle contents
        if [ ! -f "$APP_PATH/Info.plist" ]; then
          echo "❌ Invalid app bundle - missing Info.plist"
          exit 1
        fi
        echo "✅ App bundle is valid"
        
        # Set APP_PATH as an output for subsequent steps
        echo "APP_PATH=$APP_PATH" >> $GITHUB_ENV
        
    - name: Create IPA file
      run: |
        echo "📦 Creating IPA file..."
        
        # Create Payload directory
        mkdir -p ./Payload
        
        # Copy app bundle to Payload
        cp -R "$APP_PATH" ./Payload/
        
        # Create IPA file
        zip -r VividAI-Simulator.ipa ./Payload
        
        echo "✅ IPA file created: VividAI-Simulator.ipa"
        echo "📱 IPA file size: $(du -sh VividAI-Simulator.ipa)"
        
    - name: Verify IPA file
      run: |
        echo "🔍 Verifying IPA file..."
        
        if [ ! -f "VividAI-Simulator.ipa" ]; then
          echo "❌ IPA file not found!"
          exit 1
        fi
        
        echo "✅ IPA file exists"
        echo "📱 IPA file size: $(du -sh VividAI-Simulator.ipa)"
        
        # List contents of IPA
        echo "📦 IPA contents:"
        unzip -l VividAI-Simulator.ipa | head -20
        
    - name: Upload IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: VividAI-Simulator-IPA
        path: VividAI-Simulator.ipa
        retention-days: 30
        
    - name: Cleanup
      run: |
        echo "🧹 Cleaning up..."
        rm -rf ./Payload
        rm -rf ./VividAI.xcarchive
